# `core/utils.py`

## Rôle

- Fournit des helpers réutilisables (dates UTC, UUID, format ISO).

## Extension suggérée

- Validation de schémas (pydantic) si besoin.
- Fonctions de normalisation (noms de colonnes, trimming, etc.).

